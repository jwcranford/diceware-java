# diceware-java
A Java command-line tool to generate passphrases based on the diceware method at http://www.diceware.com.

## To build

$ gradle build

## To run

$ java -jar build/libs/diceware-java-0.1.jar

## To see options

$ java -jar build/libs/diceware-java-0.1.jar --help
